package com.hakim;

public class Chrono implements Runnable {

	private final int PAUSE = 3;
	public int compteurWalk = 0;
	private Profil p;

	@Override
	public void run() {
		
		while(true) {
			
			if( != null) {
				Main.scene.repaint();

				
			} else {
				SelecteHeroScreen selectedHeroScreen = new SelecteHeroScreen();
				selectedHeroScreen.repaint();
			}
			
			try {
				Thread.sleep(PAUSE);
				this.compteurWalk++;
			} catch (InterruptedException e) {
				// TODO Auto-generated catch block
				e.printStackTrace();
			}
		}
		
	}


	
	
	
}
