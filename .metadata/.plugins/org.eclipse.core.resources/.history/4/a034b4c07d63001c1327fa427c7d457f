package com.hakim;

import javax.swing.JFrame;
import java.awt.BorderLayout;

public class Main {
	
	public static Scene scene;
	public static MenuScene menuScene;

	public static void main(String[] args) {


		
		
		JFrame frame=new JFrame();
		frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
		frame.setSize(800, 600);
		frame.setLocationRelativeTo(null);
		frame.setResizable(false);
		frame.setAlwaysOnTop(true);
		
		
		menuScene=new MenuScene();
		scene=new Scene();
		scene.setSize(400, 600);
		menuScene.setSize(400, 600);
		
		frame.getContentPane().setLayout(new BorderLayout());
		
	
		frame.getContentPane().add(scene, BorderLayout.NORTH);
		frame.getContentPane().add(menuScene, BorderLayout.SOUTH);

		
		//frame.setContentPane(scene);
		//frame.add(menuScene, BorderLayout.SOUTH);
		frame.setVisible(true);

		
		
	}

}
