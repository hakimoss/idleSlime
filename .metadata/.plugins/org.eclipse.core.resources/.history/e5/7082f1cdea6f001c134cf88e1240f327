package com.hakim;

public class Chrono implements Runnable {

	private final int PAUSE = 3;
	public int compteurWalk = 0;

	@Override
	public void run() {
		
		while(true) {
			
			if(Main.scene.hero.getProfil().getSelectedHero() == null) {
				SelecteHeroScreen selectedHeroScreen = new SelecteHeroScreen();
				selectedHeroScreen.repaint();
			} else {
				Main.scene.repaint();
			}
			
			try {
				Thread.sleep(PAUSE);
				this.compteurWalk++;
			} catch (InterruptedException e) {
				// TODO Auto-generated catch block
				e.printStackTrace();
			}
		}
		
	}


	
	
	
}
